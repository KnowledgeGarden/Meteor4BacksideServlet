
Meteor.startup (function() {
  //make sure there are required fields in settings.json
  //Meteor.call('validateBackside');
  console.log(Meteor.settings.backsideURL);
//  Meteor.call('dummyAuthenticate');
//Future = Npm.require('fibers/future');
});

Meteor.methods({



});
